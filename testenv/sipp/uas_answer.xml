<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!-- UAS handling REGISTER then INVITE: answers with SDP and waits
     for the caller's BYE (up to 600 s). Media: launch SIPp with -rtp_echo
     so the agent's own RTP is echoed back to it — the recording then
     contains the played prompt, and silence follows playback EOF. -->
<scenario name="uas-answer">
  <recv request=".*" regexp_match="true">
    <action>
      <ereg regexp="REGISTER" search_in="hdr" header="CSeq:" check_it="false" assign_to="isreg" />
    </action>
  </recv>

  <nop test="isreg" next="do_register" />

  <!-- INVITE path: answer with SDP, await BYE -->
  <label id="invite_handler" />

  <send>
    <![CDATA[
      SIP/2.0 180 Ringing
      [last_Via:]
      [last_From:]
      [last_To:];tag=[pid]SIPpTag01[call_number]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Length: 0
    ]]>
  </send>

  <send>
    <![CDATA[
      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:];tag=[pid]SIPpTag01[call_number]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 1 1 IN IP4 [local_ip]
      s=-
      c=IN IP4 [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv request="ACK" timeout="30000" />
  <recv request="BYE" timeout="30000" />
  <send next="done">
    <![CDATA[
      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Content-Length: 0
    ]]>
  </send>

  <!-- REGISTER branch: 200 OK, then wait for INVITE -->
  <label id="do_register" />

  <send>
    <![CDATA[
      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:];tag=[pid]SIPpTagReg[call_number]
      [last_Call-ID:]
      [last_CSeq:]
      [last_Contact:]
      Expires: 300
      Content-Length: 0
    ]]>
  </send>

  <recv request="INVITE" next="invite_handler" />

  <label id="done" />
  <nop />
</scenario>
